# twitter-christmas
Twitter bot to make it rain prizes.
Let's make it rain.

Setup
------------
* Copy `config.template` to `config` and drop in the config settings.
* Run `python main.py`.
* Receive bacon.
